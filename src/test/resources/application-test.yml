logging:
  config: 'classpath:logback.xml'
  level:
    org:
      springframework:
        web:
          filter:
            CommonsRequestLoggingFilter: DEBUG
        ws:
          client:
            MessageTracing:
              received: TRACE
          server:
            MessageTracing:
              received: TRACE

url:
  notification-app:
    chat: http://localhost:9561/
